var libFlayer = require("../libFlayer.js");

module.exports = {
	name: 'key',
	description: 'Key',
	execute(message) {
		message.reply(
			`
			Key loading not yet available - loading from local source
			`
		);
	}
};